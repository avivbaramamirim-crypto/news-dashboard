# Configuration for RSS Feeds, Timezone, and API Endpoints

# RSS Feeds configuration
RSS_FEEDS = {
    'feed1': 'https://example.com/rss/feed1',
    'feed2': 'https://example.com/rss/feed2',
}

# Timezone configuration
TIMEZONE = 'UTC'

# API endpoints configuration
API_ENDPOINTS = {
    'endpoint1': 'https://api.example.com/v1/resource1',
    'endpoint2': 'https://api.example.com/v1/resource2',
}